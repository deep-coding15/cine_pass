# Réponse API pour une séance (film + cinéma + créneau).
class: SeanceResponse
fields:
  id: String
  cinemaName: String
  location: String
  room: String
  dateTime: String
  format: String?
  type: String?
  placesLeft: int
  placesTotal: int
  price: double
  availableOptions: List<String>?
