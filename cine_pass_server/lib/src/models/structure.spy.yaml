### Structure (cinéma, salle, organisateur) — table gérée par le schéma SQL.
class: Structure
table: cine_pass_structure
managedMigration: false
fields:
  id: UuidValue, defaultModel=random
  type: String
  name: String
  city: String
  address: String?
  website: String?
  phone: String?
  cinemaId: UuidValue?, relation(parent=cine_pass_cinema, onDelete=SetNull)
indexes:
  cine_pass_structure_type_city_idx:
    fields: type, city
