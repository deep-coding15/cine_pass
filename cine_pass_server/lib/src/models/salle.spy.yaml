### Salle d'un cinéma (pas de created_at dans le schéma).
class: Salle
table: cine_pass_salle
fields:
  id: UuidValue, defaultModel=random
  cinemaId: UuidValue, relation(parent=cine_pass_cinema, onDelete=Cascade)
  nom: String
  capacite: int
