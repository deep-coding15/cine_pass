name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.4.0'
      SERVERPOD_PASSWORD_database: 'dXjXcP4LlgeINCHHWQg9lferR8YmNKUx'
      SERVERPOD_PASSWORD_redis: 'rM75gPjdpqRz8IN1AhKGuG712P1FeP24'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: cine_pass_server
        run: docker compose up --build -d

      - name: Install dependencies
        working-directory: cine_pass_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: cine_pass_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: cine_pass_server
        run: serverpod generate

      - name: Run tests
        working-directory: cine_pass_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: cine_pass_server
        run: docker compose down -v
