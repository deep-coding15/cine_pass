name: _
publish_to: none

environment:
  sdk: '^3.8.0'

workspace:
  - cine_pass_client
  - cine_pass_server
  - cine_pass_flutter
dependency_overrides:
  flutter_secure_storage: ^10.0.0
  # Aligne test_api pour le workspace : flutter_test pinne 0.7.4, test ^1.26.3 veut 0.7.7+
  test_api: ^0.7.10

